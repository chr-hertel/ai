CHANGELOG
=========

0.14
----

 * [BC BREAK] Replace the model clients and result converters with `ListenClient` and `SpeakClient`

0.11
----

 * Add the bridge with HTTP transport for both speech-to-text (`/v1/listen`) and text-to-speech (`/v1/speak`)
