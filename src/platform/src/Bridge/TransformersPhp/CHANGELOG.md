CHANGELOG
=========

0.14
----

 * [BC BREAK] Replace the model client and result converter with `PipelineClient`

0.8
---

 * [BC BREAK] Rename `PlatformFactory` to `Factory` with explicit `createProvider()` and `createPlatform()` methods

0.2
---

 * Add support for embeddings

0.1
---

 * Add the bridge
